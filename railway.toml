[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python -m uvicorn agentic_security.api.server:app --host 0.0.0.0 --port 8000"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
