[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn agentic_security.api.server:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
