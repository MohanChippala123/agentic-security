[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "python -m agentic_security serve --host 0.0.0.0"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
